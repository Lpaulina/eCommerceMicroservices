input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  mutate {
    add_tag => [ "E-Commerce" ]
  }
 }

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
  }
}